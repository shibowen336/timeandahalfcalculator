# Claude Code local settings (per-machine permission allowlist; not for sharing)
.claude/settings.local.json

# Visual brainstorming companion (local mockups, not for commit)
.superpowers/

# Skill install artifacts (skills are installed globally; not part of the site)
.agents/
.claude/skills/
skills-lock.json

# Product Hunt assets (not part of the deployed site)
ph-gallery-1.png
ph-gallery-2.png
ph-logo-240.png

# OS / editor cruft
Thumbs.db
.DS_Store

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
